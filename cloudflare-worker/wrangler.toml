name = "miragedgeist-cloudinary-signer"
main = "worker.js"
compatibility_date = "2024-09-23"

# 环境变量（敏感值请用 wrangler secret 或在 Cloudflare 控制台设置，不要写进本文件）
# 本地用：`wrangler secret put CLOUDINARY_API_SECRET`
# 可在此写非敏感默认值：
[vars]
CLOUDINARY_CLOUD_NAME = "gopfeu83"
CLOUDINARY_API_KEY = "864775887227493"
# 注意：CLOUDINARY_API_SECRET 不要写在这里，用 `wrangler secret put` 注入
